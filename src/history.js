import { createBrowserHistory } from "history";
// Creates a browser history object using the "history" library
// This allows us to manage session history through the browser
export default createBrowserHistory();
